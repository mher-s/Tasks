function myForEach(param) {
  for (let i = 0; i < param.length; i++) {
    const element = param[i];
    // console.log(i);
    // console.log(element)
    // console.log(param);
  }
}

myForEach(["q", "w", "e", "r", "t", "y"]);
